---
- name: Setup and deploy AutoscalingProject on Minikube
  hosts: local
  become: yes
  vars_files:
      - group_vars/all.yml
  roles:
    - role: minikube
    - role: deploy
    #- role: monitoring
